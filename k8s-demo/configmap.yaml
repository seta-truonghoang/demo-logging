apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
  namespace: default
data:
  # Port for the application to listen on
  PORT: "8080"
  # Custom message to display inside the container
  CUSTOM_MESSAGE: "Hello from the ultra-powerful Kubernetes Cluster!"
